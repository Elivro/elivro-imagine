# Dev Tracker Skill Configuration
# Copy this file to .env and fill in the values

# Shared team API key (same key set in Vercel env vars for the Intranet)
DEV_TRACKER_API_KEY=your-shared-team-api-key

# API URL
# Production (Vercel-hosted Intranet):
DEV_TRACKER_API_URL=https://intranet.elivro.se/internal/api/dev-tracker
# Local development:
# DEV_TRACKER_API_URL=http://localhost:3000/internal/api/dev-tracker
